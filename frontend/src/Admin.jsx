import React from 'react'
import { useNavigate } from "react-router-dom";

const Admin = ({meals, setMeals, setMealId, username, password, isAdmin, setIsAdmin}) => {

 
  return (

    <div>
        <p>ADMIN</p>

    </div>
  )
}

export default Admin